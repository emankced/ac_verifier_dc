struct num {int} in
struct nums {int, int} in
let n := malloc(num, 10) in
let fib := malloc(nums, 0, 1) in
let fib2 := fib+1 in
  (while !(n+0) > 0 do
    !(n+0) := !(n+0) - 1;
    let x := !(fib+0) + !fib2 in
      !fib2 := !(fib+0);
      !(fib+0) := x
  );
  let result := !(fib+0) in
    mfree(fib);
    mfree(n);
    result
