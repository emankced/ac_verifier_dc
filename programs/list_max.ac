struct num { n: int } in
struct node { next: node, data: int } in
struct node_ref { ref: node } in

let list := malloc(node, null, 7) in
let list := malloc(node, list, 3) in
let list := malloc(node, list, 9) in
let list := malloc(node, list, 1) in
let list := malloc(node, list, 5) in
let list := malloc(node, list, 6) in

let max := malloc(num, 0) in
let next := malloc(node_ref, list) in
  (while !next.ref != null do
    let v := !(!next.ref).data in
      (if v > !max.n then
        !max.n := v
      );
      !next.ref := !(!next.ref).next
  );

  let res := !max.n in
    mfree(max);
    !next.ref := list;
    (while !next.ref != null do
      let addr := !next.ref in
        !next.ref := !(!next.ref).next;
        mfree(addr)
    );
    mfree(next);

    res
