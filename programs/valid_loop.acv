struct num {n: int} in
let x := malloc(num, 0) in
let sum := malloc(num, 0) in
	(@ invariant !x.n >= 0 ** !sum.n >= 0 @
	while !x.n > 0 do
		!sum.n := !sum.n + !x.n;
		!x.n := !x.n - 1);
	!sum.n;
	@ result >= 0 @