struct num {int} in
let n := malloc(num, 10) in
let fib := malloc(num, 0) in
let fib2 := malloc(num, 1) in
  (while !n > 0 do
    !n := !n - 1;
    let x := !fib + !fib2 in
      !fib2 := !fib;
      !fib := x
  );
  let result := !(fib) in
    mfree(fib2);
    mfree(fib);
    mfree(n);
    result
