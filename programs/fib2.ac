struct num { n: int } in
struct nums { a: int, b: int} in
let n := malloc(num, 10) in
let fib := malloc(nums, 0, 1) in
  (while !n.n > 0 do
    !n.n := !n.n - 1;
    let x := !fib.a + !fib.b in
      !fib.b := !fib.a;
      !fib.a := x
  );
  let res := !fib.a in
    mfree(fib);
    mfree(n);
    res
