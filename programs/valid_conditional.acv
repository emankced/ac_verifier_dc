struct num {n: int} in
let x := malloc(num, 10) in
	(@ invariant true @
	while !x.n != 5 do
	!x.n := !x.n - 1);
	(if !x.n < 0 then
        @ !x.n < 0 @;
		!x.n := -!x.n
	else
		@ !x.n >= 0 @);
	!x.n;
	@ result >= 0 @