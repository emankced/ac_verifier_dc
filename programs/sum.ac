struct num {int} in
struct array {int, int, int, int, int, int} in
let sum := malloc(num, 0) in
let data := malloc(array, 15, 3, 7, 10, 2, 5) in
let size := 6 in
  (for i in [0 to size-1] do
    !(sum+0) := !(sum+0) + !(data + i)
  );
  let result := !(sum+0) in
    mfree(data);
    mfree(sum);
    result
