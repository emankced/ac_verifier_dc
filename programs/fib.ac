struct num { n: int} in
let n := malloc(num, 10) in
let fib := malloc(num, 0) in
let fib2 := malloc(num, 1) in
  (while !n.n > 0 do
    !n.n := !n.n - 1;
    let x := !fib.n + !fib2.n in
      !fib2.n := !fib.n;
      !fib.n := x
  );
  let result := !fib.n in
    mfree(fib2);
    mfree(fib);
    mfree(n);
    result
