struct num {int} in
struct nums {int, int} in
let n := malloc(num, 10) in
let fib := malloc(nums, 0, 1) in
let fib2 := fib+1 in
  (while !n > 0 do
    !n := !n - 1;
    let x := !fib + !fib2 in
      !fib2 := !fib;
      !fib := x
  );
  let result := !(fib) in
    mfree(fib);
    mfree(n);
    result
